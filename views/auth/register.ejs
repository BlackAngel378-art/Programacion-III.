<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registro - Tienda Online</title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>
<body>
  <div class="auth-container">
    <div class="auth-card">
      <h1><span class="material-icons" style="font-size: 48px; display: block; margin-bottom: 1rem;">person_add</span>Registro de Usuario</h1>
      
      <% if (errors.length > 0) { %>
        <div class="alert alert-error">
          <ul>
            <% errors.forEach(error => { %>
              <li><%= error.msg %></li>
            <% }) %>
          </ul>
        </div>
      <% } %>

      <form action="/register" method="POST" class="auth-form">
        <div class="form-group">
          <label for="nombre">Nombre:</label>
          <input type="text" id="nombre" name="nombre" value="<%= formData.nombre || '' %>" required>
        </div>

        <div class="form-group">
          <label for="email">Email:</label>
          <input type="email" id="email" name="email" value="<%= formData.email || '' %>" required>
        </div>

        <div class="form-group">
          <label for="password">Contraseña:</label>
          <input type="password" id="password" name="password" required>
          <small>Mínimo 6 caracteres</small>
        </div>

        <div class="form-group">
          <label for="nivel">Tipo de Usuario:</label>
          <select id="nivel" name="nivel" required>
            <option value="usuario" <%= formData.nivel === 'usuario' ? 'selected' : '' %>>Usuario</option>
            <option value="admin" <%= formData.nivel === 'admin' ? 'selected' : '' %>>Administrador</option>
          </select>
        </div>

        <button type="submit" class="btn btn-primary"><span class="material-icons">person_add</span> Registrarse</button>
      </form>

      <p class="auth-link">¿Ya tienes cuenta? <a href="/login"><span class="material-icons" style="font-size: 16px;">login</span> Inicia sesión aquí</a></p>
    </div>
  </div>
</body>
</html>
